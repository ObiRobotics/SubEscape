using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class sphere_bool : MonoBehaviour {

	/*private bool sphere_in { get; set;} 

	void OnTriggerEnter(Collider other)
	{
		if (other.transform.CompareTag ("grabTag")) {
			sphere_in = true; 
			//Debug.Log (this.gameObject.transform.name + " in ");
		}
	}
	void OnTriggerExit(Collider other)
	{
		if (other.transform.CompareTag ("grabTag")) {
			sphere_in = false; 
			//Debug.Log (this.gameObject.transform.name  + " out ");
		}
	}*/
		
}
